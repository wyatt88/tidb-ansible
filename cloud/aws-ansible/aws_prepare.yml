---

- name: do AWS preparation
  hosts: localhost
  gather_facts: false
  pre_tasks:
    - include_vars:
        file: "{{ playbook_dir }}/vars.yml"
  roles:
    - aws
  post_tasks:
    - name: display hosts
      debug: msg="copy tidb-aws-inventory.py to tidb-ansible/ !"
